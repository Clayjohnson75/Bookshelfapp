{
  "compilerOptions": {
    "skipLibCheck": true
  },
  "extends": "expo/tsconfig.base",
  "exclude": [
    "api/**/*",
    "node_modules"
  ]
}

// Note: API routes use tsconfig.api.json to avoid Expo JSX compilation issues
